# Security

Do not commit secrets, tokens, sessions, credentials, private keys, caches, runtime state, or generated local state.

Do not read token, session, credential, or private key files unless the task explicitly requires it.

Do not print secrets or sensitive personal data in responses, logs, commits, or generated artifacts.

Do not add logs that expose tokens, credentials, cookies, authorization headers, or unnecessary PII.
